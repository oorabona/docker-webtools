# This file contains the version (or the strategy) for use by the `get-package`
# script. Note: this is only for GitHub hosted projects !

PKG_VERSION=1.0.0

# Acceptable values are :
# 1) a branch name (like `master`)
# 2) `latest` to use the latest (tagged) tarball
# 3) a specific version (e.g. vX.Y.Z)

declare -A versions
versions[wrk]=master
versions[siege]=latest
versions[httperf]=master
versions[nikto]=master
versions[sqlmap]=master
versions[WhatWeb]=master
